name: Build

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm run build
      - run: npm run verify
      - run: npm run archive
      - uses: actions/upload-artifact@v4
        with:
          name: NetronQuickLook-unsigned
          path: dist/NetronQuickLook-*.zip
